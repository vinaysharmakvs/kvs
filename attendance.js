const app=document.querySelector('#app');
const account=document.querySelector('#account');
const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const time=value=>value?new Date(value).toLocaleTimeString('en-IN',{timeZone:'Asia/Kolkata',hour:'2-digit',minute:'2-digit',hour12:true}):'—';
const arrival=value=>value?time(`2000-01-01T${value.slice(0,5)}:00+05:30`):'—';
const dayLabel=day=>new Date(`${day}T12:00:00+05:30`).toLocaleDateString('en-IN',{weekday:'long',day:'numeric',month:'long',year:'numeric'});
const mapLink=entry=>`https://www.google.com/maps?q=${encodeURIComponent(`${entry.latitude},${entry.longitude}`)}`;
const badge=(label,kind='neutral')=>`<span class="badge ${kind}">${esc(label)}</span>`;
const weekdays=['Mon','Tue','Wed','Thu','Fri','Sat','Sun'];
let me=null,report=null,selected=null,reportDay=null,offset=0,timingData=null,clockTimer=null;
async function api(action,data={}) {
 const response=await fetch('/api/attendance',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,...data})});
 let result;try{result=await response.json();}catch{throw new Error('Attendance service is not available. Contact your administrator.');}
 if(!response.ok){const error=new Error(result.error||'Please try again.');error.status=response.status;throw error;}return result;
}
function notice(message){const el=document.querySelector('#notice');el.textContent=message;el.hidden=false;clearTimeout(notice.timer);notice.timer=setTimeout(()=>el.hidden=true,5000);}
function errorAt(id,error){const el=document.getElementById(id);if(el){el.textContent=error.message;el.setAttribute('tabindex','-1');el.focus();}else notice(error.message);}
function bindForm(id,fn){document.getElementById(id)?.addEventListener('submit',async e=>{e.preventDefault();const button=e.submitter||e.target.querySelector('[type=submit]');button.disabled=true;try{await fn(new FormData(e.target));}catch(error){errorAt(`${id}-error`,error);}finally{if(button.isConnected)button.disabled=false;}});}
function formError(id){return `<div id="${id}-error" class="error" role="alert"></div>`;}
function setAccount(){account.innerHTML=me?`<span>${esc(me.staff.name)}</span><button class="text-button" id="logout">Sign out</button>`:'';document.querySelector('#logout')?.addEventListener('click',async()=>{try{await api('logout');me=null;selected=null;clearInterval(clockTimer);login();}catch(e){notice(e.message);}});}
function login(){setAccount();const admin=location.pathname.includes('admin-attendance');app.innerHTML=`<section class="card login"><div class="hero-art" aria-hidden="true">☀</div><span class="eyebrow">Kidsverse · Staff access</span><h1>${admin?'School administration':'Welcome, teacher'}</h1><p class="muted">${admin?'Enter your admin password to manage attendance and teacher timings.':'Enter your four-digit staff code to mark today’s attendance.'}</p><form id="login"><label class="field">${admin?'Admin password':'Staff code'}<div class="code-wrap"><input name="code" id="code" type="password" required maxlength="${admin?200:100}" inputmode="${admin?'text':'numeric'}" placeholder="${admin?'Admin password':'4-digit code'}" autocomplete="current-password" spellcheck="false" autocapitalize="${admin?'none':'characters'}"><button class="secondary" type="button" id="show-code" aria-label="${admin?'Show admin password':'Show staff code'}">Show</button></div></label>${formError('login')}<button class="primary wide" type="submit">Sign in →</button></form><p class="fine center">${admin?'Use the admin password configured by your school.':'Forgot your code? Contact admin. Existing older codes still work until replaced.'}</p><p class="fine center"><a href="${admin?'teacher-attendance.html':'admin-attendance.html'}">${admin?'Teacher sign-in':'Administrator sign-in'}</a></p><div class="divider"></div><p class="fine center">Personal staff access · Your location is captured only when you check in.</p></section>`;
 document.querySelector('#show-code').onclick=()=>{const input=document.querySelector('#code');const show=input.type==='password';input.type=show?'text':'password';document.querySelector('#show-code').textContent=show?'Hide':'Show';document.querySelector('#show-code').setAttribute('aria-label',`${show?'Hide':'Show'} ${admin?'admin password':'staff code'}`);};
 bindForm('login',async data=>{await api(admin?'adminLogin':'login',admin?{password:data.get('code')}:{code:data.get('code')});await load();});
}
async function load(){try{me=await api('me');offset=new Date(me.serverTime)-Date.now();setAccount();if(me.staff.role==='admin')await dashboard();else teacherScreen();}catch(e){if(e.status===401)login();else{app.innerHTML=`<section class="card loading"><h1>Attendance unavailable</h1><p>${esc(e.message)}</p><button class="primary" id="retry">Try again</button></section>`;document.querySelector('#retry').onclick=load;}}}
function teacherScreen(){clearInterval(clockTimer);const s=me.schedule;const receipt=me.entry;app.innerHTML=`<div class="teacher-heading"><span class="eyebrow">Your school day</span><h1>Hello, ${esc(me.staff.name.split(' ')[0])}</h1><p class="muted">${esc(dayLabel(me.today))}</p></div><div class="teacher-layout"><section class="card" id="teacher-card">${receipt?receiptHtml(receipt):`<h2>Daily check-in</h2>${s.kind==='scheduled'?`<div class="arrival"><span>Your approved arrival</span><strong>${arrival(s.arrival)}</strong><span class="fine">${s.grace_minutes?`${s.grace_minutes} minute grace period`:'No grace period'} · Set by school admin</span></div><div class="row"><div><span class="fine">Current school time</span><div class="clock" id="clock">${time(new Date(Date.now()+offset))}</div></div>${badge('Not marked','pending')}</div><div class="location"><div class="row"><div><h3>Your check-in location</h3><p class="fine" id="location-status">Allow location when prompted to check in.</p></div><span class="location-icon" aria-hidden="true">⌖</span></div></div><div id="checkin-error" class="error" role="alert"></div><button class="primary wide" id="checkin">Capture location & mark attendance</button><p class="fine center">Your device location will be saved with today’s check-in. No background tracking.</p>`:`<div class="arrival"><h3>${s.kind==='day_off'?'You’re not scheduled today':'Your schedule is not ready'}</h3><p class="muted">${s.kind==='day_off'?'No check-in is required for today.':'Please ask your school administrator to set your approved arrival time.'}</p></div><button class="secondary wide" id="refresh">Refresh schedule</button>`}`}</section><aside class="card guidance"><span class="eyebrow" style="color:#ffdc79">A simple start to your day</span><h2 style="margin-top:14px">Arrive. Check in.<br>Make a difference.</h2><div class="step"><b>1</b><p>Use your personal code.<br><span class="fine">Each teacher has her own access.</span></p></div><div class="step"><b>2</b><p>Allow your location.<br><span class="fine">Captured only for this check-in.</span></p></div><div class="step"><b>3</b><p>Keep your confirmation.<br><span class="fine">One attendance record per day.</span></p></div><button class="secondary wide" id="history-button">View my attendance</button></aside></div><section id="history" hidden class="card"></section>`;
 document.querySelector('#refresh')?.addEventListener('click',load);document.querySelector('#checkin')?.addEventListener('click',checkin);document.querySelector('#history-button').onclick=history;
 if(!receipt&&s.kind==='scheduled')clockTimer=setInterval(()=>{const el=document.querySelector('#clock');if(el)el.textContent=time(new Date(Date.now()+offset));},1000);
}
function receiptHtml(e){return `<div class="center"><div class="success-icon" aria-hidden="true">✓</div><h2>Attendance marked</h2><p class="muted">You’re checked in for today.</p></div><dl class="receipt"><div><dt>Teacher</dt><dd>${esc(me.staff.name)}</dd></div><div><dt>Date</dt><dd>${esc(e.day)}</dd></div><div><dt>Approved arrival</dt><dd>${time(e.approved_arrival_at)}</dd></div><div><dt>Checked in</dt><dd>${time(e.checked_in_at)}</dd></div><div><dt>Status</dt><dd>${badge(e.status==='late'?'Late':'On time',e.status)}</dd></div><div><dt>Captured location</dt><dd><a href="${mapLink(e)}" target="_blank" rel="noopener noreferrer">View map ↗</a><small class="fine" style="display:block">Accuracy ±${Math.round(e.accuracy_meters)} m</small></dd></div></dl><button class="secondary wide" disabled>Already marked today</button>`;}
async function checkin(){const button=document.querySelector('#checkin');button.disabled=true;button.textContent='Getting your location…';document.querySelector('#checkin-error').textContent='';try{
 if(!navigator.geolocation)throw new Error('Location is unavailable in this browser. Try a supported browser on your phone.');
 if(!window.isSecureContext)throw new Error('Open attendance using the secure HTTPS school website to allow location.');
 const position=await new Promise((resolve,reject)=>navigator.geolocation.getCurrentPosition(resolve,reject,{enableHighAccuracy:true,maximumAge:0,timeout:20000}));
 document.querySelector('#location-status').textContent=`Location captured · Accuracy ±${Math.round(position.coords.accuracy)} m`;
 button.textContent='Saving attendance…';const result=await api('checkin',{location:{latitude:position.coords.latitude,longitude:position.coords.longitude,accuracy:position.coords.accuracy,capturedAt:new Date(position.timestamp).toISOString()}});me.entry=result.entry;me.today=result.entry.day;teacherScreen();
 }catch(e){const messages={1:'Location permission was denied. Allow location in your browser settings, then retry.',2:'Your location could not be found. Move near a window and retry.',3:'Location took too long. Please retry.'};errorAt('checkin-error',new Error(messages[e.code]||e.message));if(button.isConnected){button.disabled=false;button.textContent='Retry location & check-in';}}}
async function history(){const el=document.querySelector('#history');el.hidden=false;el.textContent='Loading your attendance…';try{const data=await api('history');el.innerHTML=`<h2>My attendance</h2><p class="fine">Your most recent 60 check-ins</p><div class="table-wrap"><table><thead><tr><th>Date</th><th>Approved</th><th>Check-in</th><th>Status</th></tr></thead><tbody>${data.entries.length?data.entries.map(e=>`<tr><td>${esc(e.day)}</td><td>${time(e.approved_arrival_at)}</td><td>${time(e.checked_in_at)}</td><td>${badge(e.status==='late'?'Late':'On time',e.status)}</td></tr>`).join(''):'<tr><td colspan="4" class="empty">No attendance records yet.</td></tr>'}</tbody></table></div>`;el.scrollIntoView({behavior:'smooth',block:'start'});}catch(e){el.textContent=e.message;}}
function rowStatus(t){if(t.entry)return badge(t.entry.status==='late'?'Late':'On time',t.entry.status);if(!t.active)return badge('Inactive');if(t.schedule.kind==='missing')return badge('Schedule missing','pending');if(t.schedule.kind==='day_off')return badge('Day off');return badge('Not marked','pending');}
async function dashboard(){clearInterval(clockTimer);reportDay ||= me.today;report=await api('dashboard',{day:reportDay});const rows=report.teachers;const present=rows.filter(t=>t.entry).length;const late=rows.filter(t=>t.entry?.status==='late').length;const missing=rows.filter(t=>t.active&&!t.entry&&t.schedule.kind==='scheduled').length;const unscheduled=rows.filter(t=>t.active&&!t.entry&&t.schedule.kind==='missing').length;
 app.innerHTML=`<div class="page-heading"><div><span class="eyebrow">School administration</span><h1>Teacher attendance</h1><p class="muted">Every teacher. Her schedule. One daily record.</p></div><a class="secondary" href="teacher-attendance.html">Staff attendance page ↗</a></div><div class="stats"><div class="stat"><strong>${present}</strong><span>Present · includes late</span></div><div class="stat"><strong>${missing}</strong><span>Not marked</span></div><div class="stat"><strong>${late}</strong><span>Late</span></div><div class="stat"><strong>${unscheduled}</strong><span>Schedule missing</span></div></div><div class="admin-grid"><section class="card"><h2>Daily register</h2><div class="toolbar"><label class="field">Date<input type="date" id="report-day" value="${esc(reportDay)}"></label><label class="field">Find a teacher<input type="search" id="search" placeholder="Search by name"></label><button class="secondary" id="export">Export CSV</button></div><div class="table-wrap"><table><thead><tr><th>Teacher</th><th>Approved</th><th>Check-in</th><th>Status</th><th>Location</th><th></th></tr></thead><tbody id="register">${rows.map(t=>`<tr data-name="${esc(t.name.toLowerCase())}"><td><strong>${esc(t.name)}</strong>${!t.active?'<small>Inactive access</small>':''}</td><td>${t.entry?time(t.entry.approved_arrival_at):arrival(t.schedule.arrival)}${t.schedule.kind==='scheduled'&&!t.entry?`<small>+${t.schedule.grace_minutes} min grace</small>`:''}</td><td>${time(t.entry?.checked_in_at)}</td><td>${rowStatus(t)}</td><td>${t.entry?`<a href="${mapLink(t.entry)}" target="_blank" rel="noopener noreferrer">Map ↗</a>`:'—'}</td><td><button class="text-button edit-teacher" data-id="${t.id}">Manage</button></td></tr>`).join('')||'<tr><td colspan="6" class="empty">Add your first teacher to get started.</td></tr>'}</tbody></table></div><p class="fine">Times shown in IST. Location is reported by the teacher’s device. Not marked does not automatically mean absent.</p></section><div class="side-stack"><section class="card" id="timing-editor"><h2>Approved timings</h2><p class="muted">Select Manage beside a teacher to set her arrival time and working days.</p><div class="arrival"><h3>Different teachers, different times</h3><p class="fine">Create individual weekday schedules and one-day exceptions. Past check-ins keep their original approved timing.</p></div></section><section class="card"><h2>Add a teacher</h2><form id="add-teacher"><label class="field">Teacher’s full name<input name="name" maxlength="120" required placeholder="Full name"></label><label class="field">Four-digit code (optional)<input name="code" type="text" inputmode="numeric" pattern="[0-9]{4}" minlength="4" maxlength="4" autocomplete="off" placeholder="Leave blank to generate"></label>${formError('add-teacher')}<button class="primary wide" type="submit">Create teacher & code</button></form><div id="new-code"></div><p class="fine">Share the personal code privately with the teacher.</p></section></div></div>`;
 document.querySelector('#report-day').onchange=async e=>{if(!e.target.value)return;reportDay=e.target.value;try{await dashboard();}catch(err){notice(err.message);}};
 document.querySelector('#search').oninput=e=>{for(const row of document.querySelectorAll('#register tr[data-name]'))row.hidden=!row.dataset.name.includes(e.target.value.toLowerCase());};
 document.querySelectorAll('.edit-teacher').forEach(b=>b.onclick=()=>editTeacher(b.dataset.id).catch(e=>notice(e.message)));
 document.querySelector('#export').onclick=exportCsv;
 bindForm('add-teacher',async data=>{const result=await api('createTeacher',{name:data.get('name'),...(data.get('code')?{code:data.get('code')}:{})});await dashboard();showCode('new-code',result.code);await editTeacher(result.id);notice('Teacher created. Set her schedule next.');});
 if(selected&&rows.some(t=>t.id===selected))await editTeacher(selected);
}
function showCode(id,code){const el=document.getElementById(id);el.innerHTML=`<div class="code-result"><strong>Four-digit code — shown once</strong><code>${esc(code)}</code><p class="fine">Save and share privately. This code disappears when you leave this view.</p><button class="secondary copy-code" type="button">Copy code</button><button class="text-button dismiss-code" type="button">Hide</button></div>`;el.querySelector('.copy-code').onclick=async()=>{try{await navigator.clipboard.writeText(code);notice('Code copied.');}catch{notice('Select the code and copy it manually.');}};el.querySelector('.dismiss-code').onclick=()=>el.replaceChildren();}
async function editTeacher(id){selected=id;timingData=await api('getTiming',{teacherId:id});const t=report.teachers.find(t=>t.id===id);const v=timingData.versions.find(v=>v.effective_from<=me.today)||timingData.versions[0];renderEditor(t,v);}
function renderEditor(t,v){const el=document.querySelector('#timing-editor');el.innerHTML=`<span class="eyebrow">Teacher settings</span><h2 style="margin-top:8px">${esc(t.name)}</h2><form id="timing"><label class="field">Load a saved schedule<select id="version-select"><option value="">Choose version</option>${timingData.versions.map(x=>`<option value="${x.id}" ${x.id===v?.id?'selected':''}>From ${x.effective_from}</option>`).join('')}</select></label><label class="field">Effective from<input name="effectiveFrom" type="date" required min="${me.today}" value="${v&&v.effective_from>=me.today?v.effective_from:me.today}"></label><div class="week-labels"><span>Working day</span><span>Arrival / Grace minutes</span></div>${weekdays.map((name,index)=>{const day=v?.days.find(d=>d.weekday===index+1);return `<div class="week-row"><input type="checkbox" name="day${index+1}" id="day${index+1}" ${day?'checked':''} aria-label="${name} is a working day"><label for="day${index+1}">${name}</label><input type="time" name="arrival${index+1}" value="${day?day.arrival.slice(0,5):'08:00'}" aria-label="${name} arrival"><input type="number" name="grace${index+1}" value="${day?.grace_minutes||0}" min="0" max="120" step="1" aria-label="${name} grace minutes"></div>`;}).join('')}<p class="fine">Unchecked days are days off. This saves the complete week from the effective date until the next saved version. Existing check-ins are preserved.</p>${formError('timing')}<button class="primary wide" type="submit">Save approved timings</button></form><div class="divider"></div><h3>One-day override</h3><form id="override"><label class="field">Date<input type="date" name="day" required min="${me.today}" value="${me.today}"></label><label class="inline-check"><input type="checkbox" name="dayOff" id="day-off">Day off — no check-in required</label><div id="override-time"><label class="field">Approved arrival<input type="time" name="arrival" value="08:00" required></label><label class="field">Grace minutes<input type="number" name="grace" min="0" max="120" step="1" value="0" required></label></div><label class="field">Reason<input name="reason" maxlength="300" required placeholder="For example, training morning"></label>${formError('override')}<button class="secondary wide" type="submit">Save one-day override</button></form><div>${timingData.overrides.map(o=>`<div class="override-item"><strong>${esc(o.day)}</strong> · ${o.day_off?'Day off':arrival(o.arrival)}<p class="fine">${esc(o.reason)}</p><button class="text-button remove-override" data-day="${o.day}">Remove override</button></div>`).join('')}</div><div class="divider"></div><h3>Staff access</h3><form id="change-code"><label class="field">New four-digit code<input name="code" type="text" inputmode="numeric" pattern="[0-9]{4}" minlength="4" maxlength="4" required autocomplete="off" placeholder="Enter 4 digits"></label><p class="fine">Each teacher needs a different code. Saving signs her out and replaces her old code.</p>${formError('change-code')}<button class="primary wide" type="submit">Save new code</button></form><button class="secondary wide" id="reset-code" style="margin-top:12px">Generate a different code</button><div id="replacement-code"></div><button class="text-button wide" id="toggle-active">${t.active?'Deactivate':'Reactivate'} teacher access</button>`;
 document.querySelector('#version-select').onchange=e=>renderEditor(t,timingData.versions.find(x=>x.id===e.target.value));
 document.querySelector('#day-off').onchange=e=>{document.querySelector('#override-time').hidden=e.target.checked;document.querySelectorAll('#override-time input').forEach(i=>i.disabled=e.target.checked);};
 bindForm('timing',async data=>{const days=weekdays.flatMap((_,i)=>data.has(`day${i+1}`)?[{weekday:i+1,arrival:data.get(`arrival${i+1}`),grace:Number(data.get(`grace${i+1}`))}]:[]);if(!days.length&&!confirm('Save a week with no working days for this teacher?'))return;await api('saveTiming',{teacherId:t.id,effectiveFrom:data.get('effectiveFrom'),days});await dashboard();notice('Approved timings saved.');});
 bindForm('override',async data=>{await api('saveOverride',{teacherId:t.id,day:data.get('day'),dayOff:data.has('dayOff'),arrival:data.get('arrival'),grace:Number(data.get('grace')),reason:data.get('reason')});await dashboard();notice('One-day override saved.');});
 document.querySelectorAll('.remove-override').forEach(button=>button.onclick=async()=>{if(!confirm('Remove this override and use the weekly schedule?'))return;button.disabled=true;try{await api('deleteOverride',{teacherId:t.id,day:button.dataset.day});await dashboard();notice('Override removed.');}catch(e){button.disabled=false;notice(e.message);}});
 bindForm('change-code',async data=>{const result=await api('resetCode',{teacherId:t.id,code:data.get('code')});document.querySelector('#change-code').reset();showCode('replacement-code',result.code);notice('Four-digit code updated. The old code no longer works.');});
 document.querySelector('#reset-code').onclick=async e=>{if(!confirm('Replace this teacher’s code? Her existing code and sessions will stop working.'))return;e.target.disabled=true;try{const result=await api('resetCode',{teacherId:t.id});showCode('replacement-code',result.code);}catch(error){notice(error.message);}finally{e.target.disabled=false;}};
 document.querySelector('#toggle-active').onclick=async()=>{if(!confirm(`${t.active?'Deactivate':'Reactivate'} ${t.name}’s access?`))return;try{await api('setActive',{teacherId:t.id,active:!t.active});await dashboard();notice('Access updated.');}catch(e){notice(e.message);}};
}
function exportCsv(){const cell=v=>{let s=String(v??'');if(/^[=+@\-\t\r\n]/.test(s))s="'"+s;return '"'+s.replace(/"/g,'""')+'"';};const rows=[['Date','Teacher','Approved arrival IST','Check-in IST','Status','Latitude','Longitude','Accuracy meters'],...report.teachers.map(t=>[reportDay,t.name,t.entry?time(t.entry.approved_arrival_at):arrival(t.schedule.arrival),time(t.entry?.checked_in_at),t.entry?.status||(!t.active?'inactive':t.schedule.kind==='scheduled'?'not_marked':t.schedule.kind),t.entry?.latitude,t.entry?.longitude,t.entry?.accuracy_meters])];const url=URL.createObjectURL(new Blob(['\uFEFF'+rows.map(r=>r.map(cell).join(',')).join('\r\n')],{type:'text/csv;charset=utf-8'}));const link=document.createElement('a');link.href=url;link.download=`kidsverse-attendance-${reportDay}.csv`;link.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
load();

let refreshingDay=false;
setInterval(async()=>{if(!me||me.staff.role!=='teacher'||refreshingDay)return;const currentDay=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Kolkata',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date(Date.now()+offset));if(currentDay!==me.today){refreshingDay=true;try{await load();}finally{refreshingDay=false;}}},30000);
